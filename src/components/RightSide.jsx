import { styled } from "styled-components"

const RightSide = () => {
  return (
    <Container>
        right side
    </Container>
)
}

const Container = styled.div`
  grid-area: rightside;
`;

export default RightSide
